# Prompt: Build a complete HA MQTT system plan using RabbitMQ Cluster
system_plan:
  name: "RabbitMQ Cluster HA MQTT"
  platforms: ["Linux", "Windows", "Raspberry Pi"]
  components:
    - ErlangRuntime:
        description: "Required runtime for RabbitMQ"
        size: "20-50 MB"
    - RabbitMQCluster:
        description: "Clustered RabbitMQ nodes with quorum queues"
        size: "15-30 MB per node"
        features: ["MQTT plugin enabled", "Quorum queues", "Mirrored queues", "Failover automatic"]
    - LoadBalancer:
        description: "Optional HAProxy/NGINX for VIP access"
        size: "10-20 MB"
    - BufferPublisher:
        description: "SQLite buffer for zero data loss"
        size: "5-10 MB"
  guarantees:
    - HighAvailability: true
    - Failover: native
    - ZeroDataLoss: true
  complexity: "Medium-High"
